import { IntersectionObservableDirectiveDirective } from './intersection-observable-directive.directive';

describe('IntersectionObservableDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IntersectionObservableDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
